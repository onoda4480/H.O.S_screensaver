元のHOSプログラムのようなレトロなターミナル風にするなら、どの調整が良いか教えてください： A案: スムーズなスクロールをやめて瞬時に移動
// scroll_offset を常に 0 にする
let scroll_offset = 0;
B案: フレームレートを下げる
window.set_target_fps(15); // 60 → 15
C案: 行の追加間隔を短くする
Duration::from_millis(100) // 1秒 → 100ms
D案: すべて組み合わせる（一番レトロな感じ）
スクロールアニメーションなし
フレームレート: 15-30FPS
行の追加: 100-200ms
