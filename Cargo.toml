[package]
name = "hos_screensaver"
version = "0.1.0"
edition = "2021"

[dependencies]
winit = "0.29"
pixels = "0.13"
rand = "0.8"

[target.'cfg(windows)'.dependencies]
windows = { version = "0.58", features = ["Win32_Foundation", "Win32_UI_WindowsAndMessaging"] }
